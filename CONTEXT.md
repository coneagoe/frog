# Monitor Context

监控目标页面的领域词汇与关系，供后续产品和实现讨论保持一致。

## Language

**监控目标**：需要被系统持续观察并产生健康状态或告警信息的证券目标。

**筛选结果**：按用户当前筛选条件匹配的全部监控目标集合，而不是当前分页中的行。

**健康汇总**：针对当前筛选结果全量目标计算的状态统计。

**筛选变化**：用户修改任一筛选条件后形成新的筛选结果，并从该结果的第 1 页开始查看。

**查看位置**：用户在筛选结果中当前正在查看的分页位置；筛选条件或页大小变化会重新从第 1 页查看。

**页大小**：统一目标表一次展示的目标数量，支持 25、50、100 三档，默认 50。

**健康状态**：单个监控目标在当前数据观察窗口中的可用性与告警状态。

**空筛选结果**：当前筛选条件下不存在任何监控目标的结果。

**失效页**：原查看位置超出筛选结果最新页数的情况，通常由删除或结果集合缩小造成。

**统一目标表**：在同一查看范围内呈现全部监控目标及其健康与管理信息的目标列表。

**目标类型**：监控目标的来源类别，当前包括手工目标与工作流目标。

**页面查询状态**：当前页面持有的筛选条件、查看位置、页大小和排序；首期不通过 URL 持久化。

**管理变更**：对可管理的手工目标执行创建、更新或删除后产生的目标集合变化。

**统一查询响应**：针对一个页面查询状态同时返回当前页、健康汇总和分页元数据的完整结果。

**管理模式**：在不改变页面查询状态的前提下，为可管理手工目标显示管理操作的页面视图状态。

**刷新**：使用当前页面查询状态重新取得统一查询响应的操作。

**默认排序**：未选择显式排序时使用的稳定目标顺序。

**即时筛选**：筛选控件值变化后立即更新页面查询状态并提交查询，不需要额外确认操作。

**筛选防抖**：在连续即时筛选变化结束后短暂等待再提交查询的合并机制，当前为约 250ms。

**筛选总数**：当前筛选结果包含的全部监控目标数量，不等同于当前页行数。

**查询失败**：无法取得当前页面查询状态对应的统一查询响应的情况。

**条件类型筛选**：按手工目标配置的条件类别缩小筛选结果的条件；工作流目标不具有此属性。

**手工目标限定筛选**：只适用于手工目标的筛选条件，界面必须明确其适用范围。

**旧查询结果**：页面查询状态已经变化、但对应的新统一查询响应尚未成功返回时暂时保留的上一份结果。

**更新提示**：在旧查询结果仍展示期间，告知用户筛选结果正在重新取得的页面状态提示。

**统一页面方案**：统一目标表、健康汇总和分页共享同一页面查询状态与统一查询响应；旧接口仅作为兼容层存在。

**默认查询状态**：不限定市场、频率、启用状态或条件类型，使用默认排序、第 1 页和每页 50 个目标。

**清除筛选**：将四个筛选条件恢复为不限值并回到第 1 页，但保留当前页大小和排序。

**空筛选状态**：筛选结果为空时显示的页面状态；它不表示查询失败。

**空状态文案**：根据是否存在未筛选目标，分别说明“暂无监控目标”或“没有匹配的监控目标”。

**排序状态**：统一目标表当前采用的稳定顺序，包括默认排序、股票代码升序或股票代码降序。

**健康汇总展示**：随统一查询响应更新的只读统计区域，不作为筛选或排序控制器。

**查询进行中**：统一查询响应尚未返回时的加载状态；此时查询控件不可变更。

**查询控件**：会改变页面查询状态的筛选、排序、分页、页大小和刷新操作。

**表格排序**：只改变统一目标表当前页的行顺序，不改变筛选结果或健康汇总。

**运行状态**：监控目标当前的互斥运行状态，取 `running`、`paused` 或 `disabled` 之一。

**触发标记**：独立于运行状态，表示监控目标最近是否处于触发状态。

**运行状态优先级**：同一监控目标需要选择单一运行状态时采用 `disabled` 高于 `paused` 高于 `running` 的顺序。

**健康汇总响应式布局**：窄屏下健康统计项使用单列或两列排列，标签和数值保持完整可读。

**分页区域**：展示筛选总数、页大小选择和当前页导航的统一目标表操作区域。

**分页可访问性**：分页区域以导航语义呈现，按钮具有明确名称和原生禁用状态，当前页以文本表达。

**结果更新提示**：查询完成后以非打断式方式向辅助技术用户宣布结果已更新，不抢占当前阅读焦点。

**结果状态播报**：统一查询完成后，以非打断式方式播报筛选总数或空筛选状态；查询失败时播报错误而非成功更新。

**有效查询响应**：同时具备可用当前页、健康汇总和分页信息的统一查询响应；前端不重新推导其统计含义。

**待更新结果**：页面查询状态已变化但新的统一查询响应尚未成功替换前，暂时可见的上一份结果。

**统一翻页**：改变查看位置时重新取得包含当前页、健康汇总和分页元数据的完整统一查询响应。

**失效页规范化**：当请求页超出当前筛选结果范围时，采用后端返回的最后有效页，不把它视为查询错误。

**实时查询**：每次刷新、翻页、排序或筛选都取得最新统一查询响应，不使用客户端缓存。

## Relationships

- 一个**筛选结果**包含零个或多个**监控目标**。
- 一个**健康汇总**描述一个**筛选结果**，不只描述当前页。
- **筛选变化**会产生新的**筛选结果**，其查看位置从第 1 页开始。
- 一个**筛选结果**对应一个**查看位置**，页大小变化也会重置该位置。
- **查看位置**受**页大小**影响，但页大小只描述每页数量，不改变筛选结果本身。
- **健康汇总**统计**健康状态**，其范围由当前**筛选结果**决定。
- **空筛选结果**与**失效页**是不同情况，分别表示没有匹配目标和查看位置已过期。
- **统一目标表**展示全部类型的**监控目标**，其筛选结果和健康汇总使用同一范围。
- **目标类型**用于展示来源和管理权限，不作为首期筛选条件。
- **页面查询状态**定义统一目标表和健康汇总所使用的筛选结果范围。
- **管理变更**后，统一目标表和健康汇总按既有**页面查询状态**重新加载。
- 一个**统一查询响应**同时描述一个**筛选结果**的当前页和健康汇总，三者不得来自不同查询。
- **管理模式**不改变**页面查询状态**；仅实际**管理变更**会触发重新加载。
- **刷新**保留**页面查询状态**；浏览器重新加载页面才恢复默认查询状态。
- **默认排序**是**页面查询状态**的一部分；切换排序会从第 1 页查看新的筛选结果顺序。
- **即时筛选**会产生新的**筛选结果**；连续请求中只采用最后一次查询结果。
- **筛选防抖**只合并查询时机，不改变即时筛选的结果语义。
- 一个**筛选总数**描述一个**筛选结果**，并与其健康汇总使用相同范围。
- **查询失败**后保留上一份**统一查询响应**，并允许以相同**页面查询状态**重试。
- **条件类型筛选**激活时，所有**工作流目标**均不属于筛选结果。
- **条件类型筛选**是一个**手工目标限定筛选**，其界面标签为“条件类型（仅手工目标）”。
- **旧查询结果**只在新查询完成前暂时展示，不能被当作当前页面查询状态的最终结果。
- **更新提示**说明**旧查询结果**正在被替换，不改变筛选结果或健康汇总的定义。
- **统一页面方案**是 Monitor 页面交互的权威语义，独立分页健康视图不再是页面方案。
- 页面首次打开或浏览器重新加载时使用**默认查询状态**。
- **清除筛选**会回到未限定筛选的查询，但不等同于恢复全部**页面查询状态**。
- **空筛选状态**保留筛选操作入口，并与**查询失败**明确区分。
- **空状态文案**区分全局无目标与当前筛选无匹配两种情况。
- **排序状态**在页大小变化、刷新和清除筛选时保留，浏览器重新加载时恢复默认排序。
- **健康汇总展示**只反映当前**筛选结果**，不提供反向筛选入口。
- **查询进行中**时，筛选、分页、页大小和排序不可操作；管理模式仍可切换。
- **查询控件**在**查询进行中**不可操作，以避免页面同时存在多个未完成查询状态。
- **表格排序**不改变**健康汇总展示**的统计范围或数值。
- 一个**监控目标**只有一个**运行状态**；**触发标记**不取代或改变该状态。
- **运行状态优先级**只决定互斥状态的展示，不影响独立的**触发标记**。
- **健康汇总响应式布局**不使用横向滚动；横向滚动仅用于目标表。
- **分页区域**在窄屏下可换行，信息和操作仍保持完整可读。
- **分页可访问性**属于统一目标表交互验收范围。
- **结果更新提示**不改变焦点，也不阻断用户继续阅读已加载内容。
- **结果状态播报**包含筛选总数，空结果和查询失败使用不同语义。
- **有效查询响应**由后端统一提供；结构异常按**查询失败**处理。
- **待更新结果**与**更新提示**同时存在时，不应被误认为已反映新的筛选条件。
- **统一翻页**不单独刷新或计算健康汇总，三部分随同一响应整体替换。
- **失效页规范化**静默发生，页面使用响应中的规范化页码和分页信息。
- **实时查询**优先于复用旧的查询响应；旧结果只在新请求完成前作为暂存内容显示。

## Example dialogue

> **Dev:** “健康汇总是当前页的统计吗？”
> **Domain expert:** “不是；它必须代表当前筛选结果中的全部监控目标。”

## Flagged ambiguities

- “表格结果”可能指当前页，也可能指筛选后的全量集合；本上下文中分别称为“当前页”和“筛选结果”。

## Paper Trading Browser Access

**稳定浏览器公有源站**：用户访问纸面交易浏览器界面的唯一、长期有效的 HTTPS 域名。

_Avoid_: 将临时 Quick Tunnel URL 称为生产访问地址。

**全局会话撤销**：使一个用户的全部已签发浏览器会话立即失效的认证操作。

_Avoid_: 将其称为仅退出当前浏览器。

### Relationships

- **全局会话撤销**适用于同一用户的所有浏览器会话。

### Authentication Failure Evidence

**认证限流证据**：仅供运维识别登录请求因速率限制被拒绝的非敏感服务端证据。

_Avoid_: 将认证限流证据直接展示给浏览器用户。

**认证服务不可用**：登录依赖或认证配置故障导致登录无法安全完成的状态。

_Avoid_: 将其当作凭证错误处理。

**请求追踪编号**：用于将用户可见的系统故障响应与非敏感服务端证据关联的随机编号。

_Avoid_: 将邮箱、用户 ID、令牌或凭证作为追踪编号。

**请求追踪头**：承载请求追踪编号的统一 HTTP 头 `X-Request-ID`。

_Avoid_: 接受客户端自行提供的 `X-Request-ID` 作为可信关联值。

**认证代理信任边界**：后端只接受来自私有认证代理网络的请求追踪头；其他请求来源的追踪头均不可信。

_Avoid_: 将网络外部提交的追踪头当作可靠诊断关联值。

**会话失效**：后端确认浏览器会话缺失、格式无效、已过期或已被全局撤销的认证状态。

_Avoid_: 将数据库、Redis 或配置故障称为会话失效。

**认证代理故障**：前端同源代理无法安全定位或连接认证后端的系统故障。

_Avoid_: 将认证代理故障展示为凭证错误或暴露内部部署原因。

**认证失败证据**：描述认证请求结果及其安全运维诊断信息的结构化、非敏感记录。

_Avoid_: 在认证失败证据中记录密码、令牌、Cookie、邮箱或用户 ID。

**浏览器可见响应**：认证请求直接返回给该单用户项目使用者的有限错误信息。

_Avoid_: 将服务端诊断字段全部暴露在浏览器响应中。

**认证失败公开响应**：凭证错误、未验证用户和认证限流共同使用的 `401 AUTHENTICATION_FAILED` 响应。

_Avoid_: 通过响应体、追踪编号或 `Retry-After` 暴露这三类失败之间的差异。

**认证事件码**：用于统一分类认证失败及会话故障的固定代码集合。

_Avoid_: 由各服务自由创建同义事件码。

**认证结果类别**：将认证请求归为 `failure`、`rejected`、`unavailable` 或 `invalidated` 的固定结果集合。

_Avoid_: 使用自由文本替代认证结果类别。

**认证证据聚合**：对重复的认证限流事件按限流窗口合并记录，同时逐次保留普通凭证失败证据的策略。

_Avoid_: 对所有认证失败无差别采样，或让限流重试无限制造日志记录。

**限流证据维度**：认证限流聚合使用事件码、限流规则、实例和固定时间窗口；定位攻击所需的客户端 IP 只能使用有期限的不可逆哈希。

_Avoid_: 在认证失败证据中保存原始 IP、邮箱或用户 ID。

**认证漂移原因**：表示认证配置或运行环境不一致的有限原因集合，包括 `jwt_key_mismatch`、`schema_mismatch`、`release_mismatch`、`clock_drift` 和 `dependency_not_ready`。

_Avoid_: 为认证漂移原因新增浏览器可见事件码。

**认证证据时间**：认证失败证据使用 UTC RFC 3339 时间；`occurred_at` 表示实例处理请求的时间，`window_started_at` 仅表示限流聚合窗口的起点。

_Avoid_: 使用客户端提交时间作为诊断依据。

**认证部署诊断字段**：认证失败证据中必填的 `deployment_version`、`config_fingerprint` 和 `jwt_key_version`，分别标识发布版本、配置不可逆指纹和密钥版本。

_Avoid_: 记录密钥内容、连接串或完整配置。

**认证配置指纹**：对部署提供的显式非敏感认证配置版本字段进行规范化后计算的 SHA-256 摘要。

_Avoid_: 对完整配置、密钥或低熵秘密直接计算或记录摘要。

**认证证据输出**：认证失败证据以固定字段的单行 JSON 写入标准输出，由容器运行环境负责采集和保留。

_Avoid_: 新增认证日志文件、业务数据库表或指标基础设施作为此证据通道。

**认证依赖状态**：认证失败证据中的固定状态对象，包含代理、Redis、数据库和认证配置，分别使用 `ready`、`unavailable`、`not_applicable` 或认证配置的 `invalid` 表示请求路径已知状态。

_Avoid_: 为生成认证失败证据额外探测依赖。

**认证请求路径**：认证失败证据中的允许列表规范化路由模板，例如 `/api/auth/login` 或 `/api/paper/*`。

_Avoid_: 记录查询参数、完整 URL、动态路径段或请求体。

**认证证据关联要求**：`request_id` 在系统故障和会话失效证据中必填，在凭证错误、未验证用户和限流证据中可为空。

_Avoid_: 为反枚举场景向浏览器公开请求追踪编号。

**故障追踪响应**：系统故障和会话失效响应同时在 `X-Request-ID` 响应头与 JSON `request_id` 字段返回同一请求追踪编号。

_Avoid_: 在凭证错误、未验证用户或限流响应中返回请求追踪编号。

**会话失效公开响应**：仅受保护接口使用的 `401 SESSION_INVALID` 响应，提示重新登录、提供请求追踪编号并清除认证 Cookie。

_Avoid_: 在登录端点使用会话失效公开响应。

**认证不可用公开响应**：认证依赖、配置或代理故障共同使用的 `503 AUTH_UNAVAILABLE` 响应，提示稍后重试并提供请求追踪编号。

_Avoid_: 按 Redis、数据库、配置或代理原因改变浏览器响应。

**认证代理响应规范化**：前端认证代理将后端不可用响应统一转换为仅含公开消息、`request_id` 和 `X-Request-ID` 的 `503 AUTH_UNAVAILABLE` 响应。

_Avoid_: 将后端内部原因、状态细节或原始错误体透传给浏览器。

### Relationships

- **认证限流证据**与**认证服务不可用**均属于服务端故障证据，不改变凭证错误的通用浏览器响应语义。
- **请求追踪编号**仅随系统故障响应提供，不随凭证错误或认证限流响应提供。
- **请求追踪编号**优先由最外层认证代理生成并向后端透传；后端在缺失时兜底生成，并在日志、结构化故障响应和浏览器展示中保持一致。
- **请求追踪头**由认证代理覆盖客户端同名输入后透传；后端直连时自行生成，系统故障响应可同时返回 `X-Request-ID` 和结构化 `request_id`。
- **认证代理信任边界**要求后端对非私有代理来源忽略传入的 `X-Request-ID` 并生成新的请求追踪编号。
- **会话失效**确认后可以清除浏览器认证 Cookie；依赖故障导致的系统不可用不得清除 Cookie。
- **认证代理故障**对用户统一显示认证服务不可用并提供**请求追踪编号**；具体配置错误或后端不可达原因只进入运维证据。
- **认证代理故障**对外统一使用 `503 AUTH_UNAVAILABLE`；其内部**认证事件码**仍为 `AUTH_PROXY_MISROUTED`，并使用有限原因枚举定位具体故障。
- **认证失败证据**至少关联事件码、结果、HTTP 状态、请求路径、时间戳、服务版本、配置指纹、实例标识、依赖状态和脱敏原因；可关联**请求追踪编号**。
- **认证事件码**包括 `AUTH_INVALID_CREDENTIALS`、`AUTH_RATE_LIMITED`、`AUTH_UNAVAILABLE`、`SESSION_INVALID` 和 `AUTH_PROXY_MISROUTED`，分别表示凭证/验证状态不可区分的失败、限流、认证依赖不可用、已确认的会话失效，以及前端代理配置或连通性故障。
- **认证失败证据**使用**认证结果类别**区分结果层级：凭证或限流为 `rejected`，依赖、配置或代理不可用为 `unavailable`，已确认的会话失效为 `invalidated`，其他安全失败为 `failure`。
- **认证失败公开响应**对凭证错误、未验证用户和限流保持完全相同的 HTTP 状态、代码、消息和结构；这些情况只在**认证失败证据**中区分。
- **会话失效**不是登录阶段的反枚举失败；其 `401 SESSION_INVALID` 响应必须提供**故障追踪响应**，以支持诊断已过期、格式无效或已撤销的会话。
- **认证证据聚合**不削弱安全事件记录：普通凭证失败逐次记录，重复限流事件按限流窗口聚合，日志系统仍可按事件码统计。
- **限流证据维度**不得包含原始客户端 IP、邮箱或用户 ID；IP 哈希必须有明确过期时间。
- **认证漂移原因**统一使用 `event_code = AUTH_UNAVAILABLE` 与 `outcome = unavailable`，只出现在**认证失败证据**中。
- **认证证据时间**不依赖客户端时钟；限流证据可同时记录 `occurred_at` 与 `window_started_at`。
- **认证部署诊断字段**只保存版本号或不可逆指纹，用于定位多实例发布与配置漂移。
- **认证配置指纹**不承担 JWT 密钥漂移诊断；该责任由 `jwt_key_version` 单独承担。
- **认证证据输出**的保留目标为至少 30 天，属于部署环境配置而非应用内清理职责。
- **认证依赖状态**只反映当前请求已观察到的状态，不应增加故障期间的依赖调用或响应延迟。
- **认证请求路径**只保留定位所需的路由模板，不携带敏感参数或业务标识。
- **认证证据关联要求**同时约束服务端证据和浏览器公开范围：可诊断故障必须可关联，反枚举失败不得借此暴露差异。
- **故障追踪响应**让浏览器、代理和日志工具使用同一关联值；反枚举失败不提供该值。
- **会话失效公开响应**属于**故障追踪响应**，且与登录端点的**认证失败公开响应**保持明确边界。
- **认证不可用公开响应**属于**故障追踪响应**；其具体依赖、配置或代理原因只出现在**认证失败证据**中。
- **认证代理响应规范化**使前端代理与后端直连遵守同一浏览器错误契约。
- 本项目只有单一使用者；**浏览器可见响应**与**认证失败证据**是同一使用者在不同诊断渠道看到的信息，不代表不同用户角色或权限层级。

## Paper Trading Data Recovery

**A 股 BFQ 精确日期缺口**：纸面交易回放或估值所需的 A 股不复权日线在指定交易日期不可用的情况。

_Avoid_: 将港股、ETF 或前/后复权数据缺口混入本恢复语义。

**待恢复缺口**：已被记录但尚未取得并验证可用日线的 A 股 BFQ 精确日期缺口。

_Avoid_: 将单次重试失败称为最终失败；它仍属于待恢复缺口。

**批量恢复窗口**：工作日历史数据下载完成后，集中处理当前已记录的待恢复缺口的运维时点。

**已升级缺口**：在连续三个批量恢复窗口未解决，或自首次发现起已超过五个工作日的待恢复缺口。

**缺口升级告警**：发送给运维的 SMTP 通知，说明已升级缺口及其恢复证据。

**恢复验证**：根据提供方结果和日线数据校验确认缺口数据可信、可持久化且可用于后续重建的过程。

**缺口恢复**：恢复验证通过后，对受影响账户从最早受影响日期开始重建账本并重算相关快照的过程。

**人工审批**：运维人员针对已升级缺口作出的、允许或拒绝缺口恢复的明确决定。

**认证审批人**：作出人工审批的当前已认证单一系统用户，其用户 ID 为审计主身份，并保存审批时的名称或邮箱快照。

**永久未解决缺口**：因人工拒绝或长期无法完成可信验证而停止自动恢复、但仍保留完整证据的缺口。

**受影响业务对象**：依赖缺口日线的未成交订单、持仓估值快照或其他纸面交易派生状态。

**重新打开**：将永久未解决缺口恢复为可自动处理状态并开始新的失败计数周期的运维操作。

**升级告警周期**：从缺口达到升级阈值到解决或重新打开前的一次告警生命周期。

**审批操作**：当前已认证单一系统用户通过既有认证 API 或 CLI 对已升级缺口执行批准、拒绝或重新打开的动作。

**恢复执行时机**：人工批准后的缺口默认在下一次批量恢复窗口处理，也可由人工触发同一批量入口提前处理。

**缺口审批单元**：由业务日期、市场、证券标识和复权类型唯一确定的单个数据缺口。

**候选恢复数据**：已通过数据验证、但在已升级缺口获批前尚未写入业务数据存储的日线数据。

**候选恢复证据**：永久保存候选恢复数据、来源、验证结果和内容摘要的不可变恢复证据。

**批量恢复结果**：一次批量恢复窗口对所有缺口处理结果的汇总状态。

**统一恢复任务**：在一个下载批次的所有分区完成并聚合结果后，统一扫描、验证和处理缺口的单一任务。

**恢复阶段顺序**：候选汇总验证、已批准数据写入、账户账本重建、账户快照重算、结果审计与告警的固定处理顺序。

**恢复契约边界**：#109 负责缺口生命周期和业务恢复编排，#110 负责提供方选择、数据校验、冲突处理和幂等日线写入。

**恢复 DAG 任务**：历史数据下载和撮合完成后独立执行统一恢复的 DAG 任务，不与撮合任务合并。

**统一恢复入口**：承载缺口扫描、数据恢复和业务派生状态恢复的现有恢复服务扩展入口。

**候选版本**：由规范化日期、证券、OHLCV、成交量、成交额和来源内容摘要唯一识别的候选恢复数据版本。

**缺口状态流转**：缺口从待恢复、数据已恢复、已升级、永久未解决到重新打开的生命周期。

**账户业务恢复状态流转**：受影响账户从待处理、账本已恢复到业务恢复完成的生命周期，失败步骤保持待处理。

**审批状态**：已升级缺口取得候选数据后等待人工决定的待审批状态。

**业务恢复重试**：针对账户业务恢复状态中尚未完成的账本或快照步骤进行的幂等重试。

**业务恢复告警**：账户业务恢复连续失败达到升级阈值后产生的一次 SMTP 告警。

**业务恢复告警周期**：从账户业务恢复达到升级阈值到业务恢复完成前的一次告警生命周期。

**缺口恢复记录**：承载缺口生命周期、候选版本、审批状态和当前恢复摘要的持久化记录。

**恢复尝试记录**：一次批量恢复窗口对一个缺口执行的数据、验证、写入或业务恢复结果的不可变持久化记录。

**账户恢复记录**：一个缺口影响的单个账户的账本与快照恢复状态及结果记录。

**自动恢复门槛**：普通待恢复缺口通过验证即可自动写入和恢复，只有已升级缺口必须等待人工审批的规则。

**缺口查询入口**：通过既有认证 API 或 CLI 只读查看缺口状态、恢复次数、候选版本、审批状态、受影响账户和最近错误的入口。

**独立恢复事务**：将数据写入、单账户账本重建和单账户快照重算分别提交的恢复事务边界。

**审批版本绑定**：人工批准与候选版本摘要绑定、仅允许该版本写入的规则。

**A 股 BFQ 缺失补下载**：对已知缺失的 A 股不复权日线，由一个统一任务按现有历史下载流程重新下载并补回；按现有提供方顺序使用首个成功结果，不扩展为跨提供方比较、审批或已有数据修复。

**补下载范围**：只处理 #109 传入的缺失股票和日期；已有行直接跳过，港股、ETF、QFQ/HFQ 使用各自独立规则。

**单一补下载任务**：每个恢复批次只运行一个补下载任务，顺序处理传入的缺失股票和日期，不拆分为多个 task。

**恢复系统错误**：发生在数据验证、写入、账本重建或快照重算阶段、而不是数据仍不可用本身的恢复失败。

**无业务影响缺口**：当前没有订单或持仓依赖、因此不需要执行纸面交易恢复的已记录缺口。

**数据已恢复**：日线通过恢复验证、成功写入并可按目标键回读的状态。

**业务恢复完成**：所有受影响账户的账本、持仓、成交和快照重建及一致性检查均通过的状态。

**业务恢复待处理**：缺口数据已经恢复，但至少一个受影响账户尚未完成账本或快照重建的状态。

**账户重建范围**：从该账户最早受影响日期开始，延续到当前最新日期的完整派生状态重建范围。

**快照重算范围**：与账户重建范围相同、从最早受影响日期覆盖到当前最新日期的账户交易日集合。

**恢复截止交易日**：恢复窗口中最近一个已确认数据可用的交易日，而不是系统当前自然日。

**恢复证据**：描述一次缺口恢复尝试及其决策依据、数据结果和业务影响的不可变记录。

### Relationships

- **A 股 BFQ 精确日期缺口**由撮合或快照处理首次发现，并可进入后续恢复与升级流程。
- **待恢复缺口**在发现时持久化并去重，供后续批量恢复，而不是随每个订单单独反复请求。
- **批量恢复窗口**处理所有未解决的待恢复缺口；人工触发仅用于自动窗口之外的补偿处理。
- **待恢复缺口**达到升级阈值后成为**已升级缺口**，需要人工处理。
- 每个**已升级缺口**产生一条**缺口升级告警**；告警包含缺口标识、尝试次数、最近提供方结果和受影响账户数量。
- 每个**升级告警周期**只在首次达到升级阈值时发送邮件；后续窗口仅更新任务摘要和**恢复证据**，重新打开后再次达到阈值才产生新的邮件。
- **恢复系统错误**立即使批次失败并触发 SMTP 告警；失败账户单独回滚，其他账户仍按账户隔离规则处理，数据仍不可用的缺口继续累计恢复计数。
- **无业务影响缺口**保留诊断和恢复证据后停止自动处理；未来出现新的订单或持仓依赖时，自动重新纳入批量恢复。
- **缺口恢复**按账户执行；账本重建和快照重算分开提交，账本成功后即使快照失败也保留账本结果，失败的快照重算进入业务恢复待处理。
- 受缺口影响的账户使用**账户重建范围**，不能只重建缺口当天；后续批量窗口只处理尚未完成的业务步骤。
- **快照重算范围**与**账户重建范围**一致；现有快照重算能力可复用，恢复编排负责提供该范围。
- **账户重建范围**和**快照重算范围**均截止于**恢复截止交易日**，不使用周末、节假日或尚未完成数据的当日作为边界。
- 同一批次的所有缺口和账户共享一个按全局数据集确定的**恢复截止交易日**，不按证券分别确定。
- 批量恢复使用历史数据 DAG 的 `trade_date` 作为**恢复截止交易日**，并将其写入批量恢复记录。
- 下载批次部分证券失败时，只处理已验证可用的数据；失败证券继续保留未解决缺口，不阻塞其他证券恢复。
- 分区下载只负责获取数据和报告结果；所有分区完成后由**统一恢复任务**执行缺口扫描、候选验证、日线写入、账本重建、快照重算、审计和告警。
- **统一恢复任务**先汇总验证全部候选，再一次性幂等写入已批准日线，然后按账户重建账本和快照，最后汇总状态、审计并告警。
- #109 只消费 #110 提供的已验证候选数据或验证失败结果，不重复实现提供方优先级、OHLCV 校验、冲突处理或日线写入契约。
- **恢复 DAG 任务**依赖现有撮合任务，恢复失败可单独重试而不重复执行撮合。
- **恢复 DAG 任务**在撮合任务 `completed` 或 `completed_with_warnings` 时继续；撮合 `failed` 时跳过并等待单独重试，不重复执行撮合。
- **统一恢复入口**复用并扩展现有 delayed-bar 重建服务，不复制账本或快照重建实现；最终覆盖所有缺口、数据写入、影响扫描、分步重建、审批、证据和告警。
- **候选版本**摘要不变时，获批候选可直接重试写入和业务恢复；摘要变化时视为新候选，必须重新验证并重新审批。
- **缺口状态流转**与**账户业务恢复状态流转**相互独立；账户业务恢复失败不会使数据已恢复的缺口回退。
- 缺口达到阈值并取得候选数据后进入**审批状态**；批准后进入数据已恢复，拒绝后进入永久未解决，候选版本变化时重新进入审批状态。
- **业务恢复重试**只执行失败步骤：账本成功而快照失败时只重算快照，账本失败时先重建账本再重算快照；已恢复日线不重复下载或写入。
- **业务恢复告警**沿用连续三个批量窗口或首次失败后超过五个工作日的阈值，但独立于数据缺口计数；同一账户达到阈值只发送一次，不影响其他账户。
- 账户达到**业务恢复完成**后关闭**业务恢复告警周期**并清零失败计数；未来再次失败时开启新的周期并重新累计。
- 现有日线诊断保留为当前摘要；**缺口恢复记录**、**恢复尝试记录**和**账户恢复记录**分别承载当前状态、不可变历史和账户级进度。
- **自动恢复门槛**只约束已升级缺口；普通待恢复缺口验证通过后直接进入数据恢复和业务恢复。
- 候选日线写入按缺口隔离；单个写入失败的缺口回滚并保持未解决，其他缺口成功写入可继续恢复，但批量恢复结果为失败并触发告警。
- **缺口查询入口**为只读运维能力，不新增审批页面、角色模型或独立权限系统。
- **独立恢复事务**允许账本成功后保留、快照失败后单独重试，也允许一个账户失败而其他账户继续提交。
- **审批版本绑定**要求批准时提交候选 hash；执行前 hash 不一致则旧审批失效，缺口回到待审批。
- 缺口达到升级阈值但没有候选数据时保持已升级并发送一次缺口升级告警；取得并验证候选版本后才进入审批状态。
- 分区下载任务整体失败时跳过**统一恢复任务**，待分区修复并重试后再处理；仅分区成功但个别证券结果为 warning 时继续恢复其他已验证数据。
- 只有持仓估值依赖而没有订单依赖的缺口，也要依据 valuation gap 和账户持仓创建账户恢复记录并纳入快照重算；该路径不执行账本重建。
- 快照重算成功只关闭对应账户和日期的 valuation gap；日线诊断由数据恢复流程独立关闭，不能跨账户或全局关闭。
- 只有**恢复验证**通过后，缺口才能标记为已解决并从待恢复集合中移除。
- **已升级缺口**必须先获得**人工审批**才能执行缺口恢复；审批记录包括操作人、时间、依据和结果。
- **人工审批**必须关联**认证审批人**；调用方不能通过自由文本自报审批身份。
- 单用户系统中，当前已认证用户即可批准或拒绝已升级缺口；不引入管理员、运维或普通用户角色，也不要求第二名人员复核。
- **审批操作**复用既有认证边界，不新增审批页面、角色模型或独立权限系统；操作记录当前用户、时间、动作和结果。
- **恢复执行时机**不产生第二套恢复路径；人工提前处理仍使用批量窗口的验证、幂等、账户隔离和审计规则。
- **人工审批**以**缺口审批单元**为粒度，不以整个批量恢复任务为粒度。
- 同一**缺口审批单元**影响多个账户时，一次审批覆盖该缺口的全部账户；各账户仍分别执行并隔离失败。
- 已升级缺口先产生并保留**候选恢复数据**供审批；审批通过后才写入并执行缺口恢复，拒绝时不写入业务数据但保留验证和审批证据。
- **候选恢复证据**永久保存且不设过期时间；包含完整候选 OHLCV、来源、验证结果和内容摘要。
- 候选数据获批后若发生**恢复系统错误**，保留批准状态、候选数据和审批证据，后续直接重试写入或业务恢复；候选内容变化时必须重新验证并重新审批。
- **批量恢复结果**在数据仍不可用时为成功但带警告；发生验证、写入或业务恢复系统错误时为失败并触发 SMTP 告警。
- 已被拒绝或标记为无业务影响的缺口一旦出现新的订单或持仓依赖，自动重新纳入待恢复流程并重置连续失败计数；既有**恢复证据**不得修改。
- 只有针对缺口完成数据尝试且结果仍不可用时，才增加连续失败次数；**恢复系统错误**只单独记录并告警，不增加缺口计数。
- **永久未解决缺口**不再参与自动批量恢复；受影响订单保持 `accepted` 且不产生伪造成交，相关快照保留估值缺口。
- 只有新的**人工审批**明确批准，才能**重新打开**永久未解决缺口；重新打开不要求填写原因，重置连续失败次数但保留首次发现时间和全部历史证据。
- 批量恢复扫描所有未解决的 A 股 BFQ 缺口，并按**受影响业务对象**分流：订单缺口触发账本重建和快照重算，只有估值缺口触发快照重算，无业务影响的缺口保留证据后结束自动处理。
- 只有达到**数据已恢复**，相关数据缺口才可标记为已解决；数据写入失败则回滚本次数据恢复。
- 只有达到**业务恢复完成**，相关业务恢复待处理项才可清除；账户重建失败只回滚该账户并保留业务恢复待处理状态。
- **恢复证据**永久保留并追加写入，不因更新缺口摘要而覆盖；至少关联缺口、恢复批次、提供方结果、验证结论、受影响账户、重建结果、错误信息和人工审批信息。
- 批量恢复必须幂等：重复执行先重新读取缺口和日线状态，已解决缺口直接跳过；同一账户同时只能有一个缺口恢复重建。
- **缺口恢复**继续使用账户锁和幂等业务事件，不能产生重复成交、账本事件或快照。
- 数据缺口可在账户重建或快照重算失败时标记为已恢复；失败的账户业务步骤必须单独进入**业务恢复待处理**，后续批量窗口只重试失败步骤，不重复下载已验证日线。
- 本术语不包含港股、ETF 或 QFQ/HFQ 数据缺口；这些数据类型需要各自定义恢复语义。
